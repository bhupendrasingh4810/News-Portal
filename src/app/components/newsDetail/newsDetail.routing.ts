import { Routes } from '@angular/router';
import { NewsDetailComponent } from './newsDetail.component';

export const newsDetailRoute: Routes = [
    {
        path: '',
        component: NewsDetailComponent
    }
];